
  <form class="form edit-form" data-id="{{id}}">
    <div class="form-group">
      <label for="project-name">Post Title</label>
  <div>{{name}}</div>
  </div>
  <div class="form-group">
   <label for="description-edit">Post Content</label>
  <textarea id="description-edit" class="form-input">{{description}}</textarea>
  </div>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
 
  <p>
    <label for="project-desc">Comment</label>
    <textarea name= id="" class="form-input
    "></textarea>
    <button  type="submit" class="btn btn-primary">Create Comment</button>
     </p>
    </form>

 <button data-id="{{id}}" class="btn btn-primary delete-btn">Delete Comment</button>

<script src="/js/comment.js"></script>
